name: plis_administrador
description: "Aplicación de administración para Plis"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  english_words: ^4.0.0
  google_fonts: ^6.1.0
  permission_handler: ^11.3.1
  file_picker: ^8.0.0+1
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  cloud_firestore: ^6.0.0
  firebase_storage: ^13.0.0
  firebase_app_check: ^0.4.0
  path: ^1.8.3
  syncfusion_flutter_pdfviewer: ^28.1.36
  url_launcher: ^6.3.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
